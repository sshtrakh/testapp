import { LargeButtonDirective } from './large-button.directive';

describe('LargeButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new LargeButtonDirective();
    expect(directive).toBeTruthy();
  });
});
