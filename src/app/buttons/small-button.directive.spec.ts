import { SmallButtonDirective } from './small-button.directive';

describe('SmallButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new SmallButtonDirective();
    expect(directive).toBeTruthy();
  });
});
