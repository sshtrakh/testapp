import { MediumButtonDirective } from './medium-button.directive';

describe('MediumButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new MediumButtonDirective();
    expect(directive).toBeTruthy();
  });
});
